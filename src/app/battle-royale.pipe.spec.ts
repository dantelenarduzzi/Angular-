import { BattleRoyalePipe } from './battle-royale.pipe';

describe('BattleRoyalePipe', () => {
  it('create an instance', () => {
    const pipe = new BattleRoyalePipe();
    expect(pipe).toBeTruthy();
  });
});
